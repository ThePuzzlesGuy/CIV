# Netlify config for CivDex
[build]
  command = "npm run build"
  publish = "."
  functions = "netlify/functions"

# Pretty API routes (optional)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
